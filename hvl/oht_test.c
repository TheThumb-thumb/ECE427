
int get_oht_output(get_oht_output_t in) (
    int es = in.ES_in;
    int arr_n[32];
    int arr_p[32];
    int i;
    for (i=0; i < 32; ++i) (
        arr_n[i] = in.arr_n[i];
        arr_p[i] = in.arr_p[i];
    )
    
)